# Merge into deepresearch.local.yaml; this fragment is not a complete config.
# The host must already have these exact native tools configured and usable.
# Keep the host's provider credentials in its existing environment/config.
require_dual_source: false
sources:
  - name: public-web-search
    kind: native
    tool: web_search
    origin: external
    level: L4
    publisher: web-search-unclassified
  - name: public-web-read
    kind: native
    tool: web_fetch
    origin: external
    level: L4
    publisher: web-content-unclassified
source_fallback: [public-web-search, public-web-read]
# Also authorize web_search/web_fetch in the researcher Custom Agents.
